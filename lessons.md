# 经验教训（lessons.md）

> 记录项目运行中遇到的**框架问题与改进建议**，供框架迭代时读取学习。业务需求、项目决策不记在这里——那是 `docs/decisions.md` 的事。
> 本文件随框架复制到新项目（模板角色）；项目运行中积累的条目，由用户复制回框架仓库，供框架迭代时读取学习。

---

## 责任分工（谁记录）

- **角色（执行者）**：遇到框架问题 → 在完工回报 §8「需要 PM 关注的决策」或对话中报告，**不直接写本文件**——本文件位于项目根目录（主工作区），归 PM 专用，角色在 Worktree 中无写权限。
- **PM**：汇总角色报告，将条目写入本项目 lessons.md（与「对话决策由 PM 落盘」同一原则：不落盘 = 没发生）。
- **框架仓库 R99**：处理框架仓库 lessons.md 中的 [open] 条目。

---

## 记录格式（极简，随手可写）

每条经验一行式、三段信息，不带格式负担：

```markdown
- [open] 问题：{一句话描述}
  证据：{具体场景 / 错误输出 / 度量数字 / 复现路径}
  建议：{改哪个文件 / 哪个字段 / 哪条规则}
```

示例：

```markdown
- [open] 问题：R04 原型阶段强制 worktree 纯属浪费，原型卡应豁免
  证据：TC-003 从建 worktree 到删掉耗时 1 轮
  建议：改 docs/git-workflow.md §4，原型类卡豁免 worktree
- [open] 问题：validate-task-card.ps1 的 F3 关键词误报
  证据：验收标准写了"列表为空时显示空状态"仍被判缺边界
  建议：boundaryKw 补充"空状态"
```

## 状态约定

- `[open]`：待框架仓库处理
- `[done]`：已处理——采纳 → 注明改了什么；不采纳 → 注明理由（如 `[done] 不采纳（理由）`）

## 学习约定

1. **项目运行中**：PM 汇总角色报告，将条目写入本项目根目录 `lessons.md`（格式见上）。
2. **框架学习**：项目中的 `lessons.md` 由用户直接复制回框架仓库（覆盖或追加，无需合并/标记仪式）；框架仓库维护者（R99）在修改任何框架文档/脚本前，先读本文件，处理所有 `[open]` 条目：采纳 → 落地改动 → 标 `[done]`；不采纳 → 标 `[done] 不采纳（理由）`。处理不掉的挂起问题保留 `[open]`，不删除。
3. 处理完的条目保留在文件中（历史即轨迹），不删行。

## 条目

（空——复制到新项目后在此追加；框架仓库侧积累跨项目条目）

