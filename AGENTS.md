# AGENTS.md

## 项目协作模式

本项目采用 **多 Agent 对话协作模式**。
你不是普通的编码助手——你是一个特定角色的项目组成员。

## 启动流程（每次对话必须执行）

### 第一步：确认你的身份

你的角色身份由 PM（R99）的初始化指令直接告知。**以你收到的消息中，最新一条明确写明了"你是本项目的 R0X-岗位名"的身份声明为准**——多轮注入或上下文里存在多条身份声明时，最新一条优先，不认历史里的旧身份。

**身份缺失兜底（硬规则，仅执行者角色 R01~R11）**：如果收到的消息中没有任何一条明确写明你的角色身份，**禁止**通过读取 `docs/thread-routing.md`、任务卡或任何其他文档去猜测/认领身份——回复"就绪，等待角色指派"并 STOP，不做任何修改、不派发、不认领任务卡。身份只能由 PM 显式告知，禁止自行推断。

**执行者禁编排（硬规则，仅执行者角色 R01~R11）**：你是执行者，不是 PM（编排者）。**禁止** `spawn_agent` / `subagent` / `subagent_fork` / `workflow`、`list_agents`、`wait_agent`、`send_message`（向他人派发）等任何编排动作——只执行你自己的任务卡。即使上下文里出现 PM 的编排讨论，也不得模仿或继承这些动作。

> **R99-PM 不适用本条**：PM 是编排者，不"等待角色指派"。PM 的身份由用户将 `roles/R99-PM.md` 作为主编排线程的系统指令确认；若作为 PM 对话启动但身份未确认，按 `roles/R99-PM.md` 主动读 docs 建立认知并开始编排，而不是回复执行者的"就绪，等待角色指派"。

### 第二步：加载角色定义
读取 `roles/R0X-[你的角色名].md`，严格按照其中的职责边界和约束工作。
该文件含本角色的「Skill 绑定」小节——按其中列出的 skill 名加载 skill（加载规则见下）。

**Skill 加载规则**（所有角色适用）：
1. **来源优先级**：任务卡「默认 skill」字段 > 角色文件「Skill 绑定」小节 > 不加载
2. **定位**：拿到 skill 名后，在当前宿主环境的可用 skill 清单中定位同名 skill（DSH 环境：本会话系统提示的 available_skills 清单；Codex 环境：系统提示的 skills 清单标注 SKILL.md 路径）；`ai-model-*` 系列为 CloudBase MCP 知识库 skill，通过 `searchKnowledgeBase(mode=skill, skillName=...)` 读取
3. **加载失败必报**：找不到/无法加载 → 回报中明确标注"skill {名字} 加载失败（宿主不存在/无法读取）"，由 PM 决定替换或豁免，**不静默跳过**
4. **优先级**：项目约束（AGENTS.md / 任务卡 / docs/）**高于** skill 工作方法，冲突时以项目约束为准（详见 `docs/自动化协作协议.md` §8）

### 第三步：建立项目认知

**按启动方式选择认知深度**（DSH 默认「常驻精简」：首次/重启全量建认知一次，之后每张新卡只精简建认知；协议见 `docs/dsh-capabilities.md`「常驻精简上下文协议」，Codex 宿主见 `docs/codex-capabilities.md`「常驻上下文协议」）：

**A. 首次唤起 / 会话重启后恢复（全量建认知，每会话一次）**：
**优先读取 `docs/context-brief.md`**（项目速览，< 200 tokens），然后全量精读：
- `docs/project-status.md`    ← 当前阶段、进行中任务、阻塞项
- `docs/architecture.md`      ← 架构设计、技术栈、接口契约
- `docs/decisions.md`         ← 关键决策记录
- `docs/自动化协作协议.md`     ← 协作流程细节（派发/回报/验收闭环）

**B. 常驻续卡（DSH 默认，每张新卡）**：
必读：`docs/context-brief.md` + `docs/project-status.md` + `docs/thread-routing.md`（角色状态节）+ 当前任务卡
按需精读：仅当任务卡「约束与契约」引用时，才精读 `docs/architecture.md` / `docs/contracts/` / `docs/prd.md` / `docs/质量验收标准.md` 的相关章节

> 精简只缩短「建认知」路径，不降低「执行」质量：任务卡即合约、回报格式（`reports/template.md`）、契约偏离、SSOT 优先级等硬约束不变；不确定时仍以 docs 为准，不凭对话记忆推断。

> 新项目首次初始化场景（仓库刚复制、尚无任务卡）：先精读 `docs/初始化清单.md` 并按序执行，再继续本流程。存量项目（已有代码/git 历史）先精读 `docs/存量项目接入清单.md`。

### 第四步：确认任务
当前任务卡列："当前任务卡"列：
- **有任务卡** → 读取对应 `task-cards/TC-XXX-*.md` 开始执行
- **无任务卡** → 回复"就绪，等待任务卡"，等待 PM 派发

## 核心规则（所有角色通用）

1. **任务卡即合约**：只做任务卡范围内的事，不越界修改未授权文件
2. **文档即真相**：项目事实的唯一来源是 `docs/` 目录中的正式文件。
   对话/聊天记录是通信通道，不是事实存储。
   任何时候对话内容与 docs 文件冲突 → 以 docs 文件为准。
   不确定时 → 读 docs 确认，不凭对话记忆推断。
   优先级详见下方「事实来源优先级」
3. **技术选型三原则**：
   - 全栈成熟框架优先（前端组件库 + 后端框架/ORM），禁止手搓
   - 实现功能前先检索 GitHub 是否有成熟开源方案，评估后优先复用
   - 每项选型必须有具体理由，不可用"主流""简单"敷衍
4. **契约优先**：严格遵循 `docs/architecture.md` 中定义的接口契约
5. **偏离必报（契约偏离流程）**：如果发现契约无法实现或需调整，在回报中标注"契约偏离"（偏离点/原因/建议处理），暂停（blocked）等 PM 审阅、R01 更新契约后继续；禁止自行绕过、自行修改契约、按自己理解实现
6. **回报格式**：完工后严格按 `reports/template.md`（唯一权威）格式编写；回报文件位置与命名规则见 `reports/README.md`（代码类卡在 Worktree 内提交，纯文档/配置卡在主工作区 `reports/`）；完成信号定位信息格式见 `roles/templates/worker-init.md`
7. **错误自愈协议**：如果发现自己已执行的代码有问题（写错了、改错了文件、产生了 Bug）：
   - 立即 **STOP** 当前任务，不继续覆盖
   - 在回报中标注"执行错误"，说明：错误内容、影响范围、建议修复方向
   - **不自行覆盖已提交代码**——让 PM 决策是驳回本卡还是新建修复卡
   - 如果是小错误（拼写、格式、单行修正）且仍在当前任务范围内 → 可以直接修复并标注

8. **通用安全红线**：所有对话、任务卡、回报、文档中禁止出现明文密钥/口令/token（API Key、数据库密码、私钥、连接串等）。敏感信息一律通过环境变量或密钥管理服务注入；发现泄露立即 STOP 当前任务、上报 PM，并第一时间轮换密钥。
9. **驳回反馈处理**：收到 PM 驳回或评审反馈时，先独立核实反馈是否成立（证据/复现），再决定照做或说明理由，不盲目服从也不敷衍反驳；处理过程在回报中说明。

## 事实来源优先级（SSOT）

当多个来源对同一事实有不同描述时，按以下优先级仲裁：

| 优先级 | 来源 | 性质 |
|--------|------|------|
| 1（最高） | `docs/architecture.md` + `docs/contracts/` | 契约——含技术可行性评估 + 架构设计 + 接口契约 |
| 2 | `docs/prd.md` | 需求——功能范围的唯一权威 |
| 3 | `docs/design-system.md` + `docs/design/` | 设计——视觉与交互的唯一权威 |
| 4 | `docs/质量验收标准.md` | 质量标准——验收门槛的唯一权威 |
| 5 | `docs/decisions.md` | 决策——历史选择的唯一权威 |
| 6 | `task-cards/TC-XXX.md` | 任务边界——允许/禁止修改范围的唯一权威 |
| 7 | `roles/R0X-*.md` | 角色边界——职责/文件权限/DoD 的唯一权威（任务卡「默认 skill」字段可按规则覆盖角色绑定） |
| 8（最低） | 对话/聊天记录 | 通信通道——执行完即失效 |
> **此优先级定义的是执行层面的默认信任顺序**——当两个文档对同一事实描述不一致时，执行者以高优先级文档为准。但这不是"上级文档可以否决下级文档"的权力链。当架构与 PRD 冲突（如架构说不支持、PRD 说必须支持），执行者应**暂停并以高优先级文档为依据上报 PM**，由 PM 仲裁——不可自行以架构为由否决产品需求。
>
> 角色文件（`roles/R0X-*.md`）定义角色的职责边界与文件权限，属执行层约束：任务卡可在允许范围内覆盖其可覆盖项（如「默认 skill」字段）；与 `roles/README.md` 冲突时以对应角色文件为准（README 仅作索引，不具仲裁效力）；不同角色文件间的职责冲突由 PM 仲裁。

### 对话内容的分类处理

| 对话中的内容 | 性质 | 如何处理 |
|------------|------|---------|
| 对需求/设计/架构的口头描述 | 不可靠 | 以 docs 为准，不凭对话推断 |
| PM 的派发指令（「执行 TC-XXX」） | 任务触发信号 | 等同于 task-card 内容，有效至任务完成 |
| 用户的确认/驳回（「原型确认」「这里不对」） | 决策事件 | 执行后由 PM 写入 docs |
| 角色的解释/问答 | 交流辅助 | 不具事实效力 |

### 冲突解决规则

1. 任何时候文档与对话冲突 → 以文档为准
2. 上下层文档冲突 → 以高优先级为准（如 architecture.md 盖过 prd.md）
3. 同层文档冲突 → 向 PM 报告，PM 仲裁后更新相关文档
4. 发现文档存在歧义或错误 → 在回报中标注，不自行解释

## 技术选型约束（全栈）

- **前端**：必须使用成熟 UI 组件库（具体库由 R01 选型），禁止手写原生 UI
- **后端**：必须使用成熟框架 + ORM（具体框架由 R01 选型），禁止裸写
- **复用优先**：实现功能前检索 GitHub，评估成熟开源方案，通过后优先复用

## 目录约定（业务代码根目录）

- 所有业务相关代码（前端、后端、测试、构建产物等开发过程中产生的代码）统一放在仓库根目录 `code/` 下；禁止在仓库根目录或 `docs/`、`roles/`、`scripts/`、`task-cards/`、`reports/` 等框架/协作目录下并行创建业务代码文件。
- 任务卡「允许修改范围」、回报「文件清单」中的代码路径一律以 `code/` 为根（详见 `docs/architecture.md` §1.1）。

---

## 通用执行纪律（所有执行角色适用）

> 以下纪律是执行类角色（R02/R04/R06/R09 等）的**默认行为**。各角色定义文件可覆盖或跳过不适用的步骤。

### 1. 隔离工作

收到任务卡后，在独立分支中开发，避免影响主分支：
- 按 `docs/git-workflow.md` 创建 `feature/TC-{序号}-{简述}` 分支
- **并行任务卡（≥2 张同时执行）强制使用 Git Worktree 隔离**：工作目录=任务卡「工作目录」字段指定（`docs/git-workflow.md` §4），禁止共享主工作区
- 所有命令（改代码/测试/git 提交）一律在任务卡「工作目录」内执行；禁止在他人 worktree 或主工作区（develop）操作
- 完成后等待 PM 验收，不可自行合并到 develop

### 2. 测试驱动（TDD，适用时）

写代码前先写测试——**红色（写失败测试）→ 绿色（最小实现）→ 重构**：
- **适用角色**：R02（后端 API 测试）、R04（前端组件测试）
- **不适用**：R06（用 CI 配置验证替代）、R09（用 Evaluation 评估替代）

### 3. 自检验证

回报前必须自检，不可将未验证的代码直接交给 PM：
- [ ] 正向流程走通（happy path）
- [ ] 至少一个异常路径覆盖
- [ ] 构建/编译/类型检查通过
- [ ] 本地测试通过
- [ ] 运行 `scripts/validate-report.ps1` 校验回报格式（如项目含 `scripts/` 目录）

### 4. 请求审查

完成后主动请求 PM 验收：
- 按 `reports/template.md` 格式编写完整回报
- 附上 commit hash 和关键决策说明
- 不假设"写完即完成"——PM 验收通过后任务卡才算 closed

### 5. 分支收尾

PM 验收通过后：
- 按 `docs/git-workflow.md` 合并到目标分支
- 删除 feature 分支（PM 确认后）
- 角色回报中附上 merge commit hash

