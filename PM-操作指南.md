# PM 编排操作指南

> **职责分工**：本文档 = PM 操作步骤手册，聚焦“怎么操作”；流程规则（派发/回报/验收/异常/模型/Skill）以 `docs/自动化协作协议.md` 为唯一权威；角色通用约束见 `AGENTS.md`。本文档与协议冲突时以协议为准。高频规则唯一权威映射见 `docs/规则权威索引.md`。
>
> **宿主工具名**：本文档用「语义动作」（唤起/注入/等待/关闭/复用）描述编排，操作步骤以 **DSH（默认宿主）工具名**落地（`subagent` / `send_message` / `list_agents` / `ask_user_question`）；历史遗留的 Codex 字样（`spawn_agent`/`send_input`/`wait_agent`/`close_agent`）按 `docs/dsh-capabilities.md`「术语速查」映射，勿按字面名调用。
>
> 给 PM 角色（人类 + AI）的操作手册

---

## 一、初始化项目（新项目第一次用）

当用户说"初始化多 Agent 协作"时：

> **机械步骤见清单**：框架文件复制、`git init` + 创建 `develop` 分支、规模级别选择、角色登记、质量裁剪、状态文档填充、首次巡检（`patrol.ps1` PASS）等步骤以 `docs/初始化清单.md` 为**唯一权威**，本节不重复。下面只列清单未覆盖的编排动作。

1. **询问项目信息**：确认项目做什么、技术栈、平台类型
2. **推荐角色组合**：根据项目类型（Web 全栈/移动端/AI·ML 等）推荐角色，让用户确认
3. **补齐缺失角色**：标准角色（R01~R09、R10、R11）已全部预置。仅当用户需要超出标准角色池的扩展角色时，执行者类以 `roles/templates/role-definition.md` 为模板生成，产出者类参考已有文件手工编写
4. **复制框架文件并建立 Git 基线**：按 `docs/初始化清单.md` 一、二节执行（复制框架文件、`git init`、`git branch develop`）
5. **唤起角色子代理（常驻）**：PM 按用户确认的角色组合**唤起角色子代理**（命名固定 `R0X-岗位名`），DSH 下用 `subagent` 一步拉起并注入初始化消息（`roles/templates/worker-init.md`）+ 绑定 skill，唤起后**保持常驻**、后续卡直接续派（常驻模型见 `docs/dsh-capabilities.md`；Codex 宿主见 `docs/codex-capabilities.md`）；手工 fallback 模式下由用户在侧边栏创建对话。
6. **派发产品设计任务卡**：PM 向 R10-产品经理 产出派发指令，PRD 验收通过后进入 UIUX 设计阶段
7. **派发 UI/UX 设计任务卡**：PM 向 R11-UIUX 设计师 产出派发指令，验收通过后进入前端原型阶段
8. **派发前端原型任务卡**：PM 向 R04-前端工程师 产出派发指令，基于设计规范搭建可交互原型，用户确认后进入架构设计阶段
9. **派发架构设计任务卡**：PM 向 R01-架构师 产出派发指令，验收通过后进入开发阶段

---

## 二、PM 对话启动 SOP

每次开设新的 PM 对话时：

### 第 1 步：初始化对话

将 `roles/R99-PM.md` 作为 PM 对话（主编排线程）的第一条系统指令。

### 第 2 步：让 AI 读取项目真相

发送以下消息：
```
请先读取以下文件，建立项目认知：
1. docs/project-status.md
2. docs/architecture.md
3. docs/decisions.md
4. docs/thread-routing.md
5. docs/自动化协作协议.md
6. task-cards/ 目录下所有未归档的任务卡
7. reports/ 目录下所有未处理的回报
8. reports/README.md（回报目录与命名规范）
9. scripts/README.md（框架工具集说明与校验规则）

读完这些文件后，请用简短语言总结：
- 项目当前处于什么阶段
- 哪些任务在进行中
- 有哪些阻塞项需要优先处理
- reports/ 中是否有未验收的回报
```

### 第 3 步：开始编排

让 AI 给出下一步建议，你来确认后执行。

---

## 三、任务卡全生命周期

### 3.0 验收标准评审（第二道防线，派发前）

> 防止 PM 单独写验收标准的盲区。**大卡**（预计复杂度=高，或涉及契约/schema/冻结产出物/外部证据项，或多角色协作）派发前必须经 R03 评审验收标准。

**流程**：
1. PM 建卡后、派发前，将「验收标准」部分发给 R03（`send_message` 注入评审请求；轻量方式，不建卡）
2. R03 按 `roles/R03-QA工程师.md`「验收标准评审」四要点评审：可测性 / 完整性（F3+F4）/ 外部证据可行性 / 契约一致性
3. 评审结论：通过 → PM 将 `acceptance_reviewed_by: R03` 写入任务卡 front-matter；需修改 → PM 修订验收标准后再派发
4. PM 判定无需评审的卡（light 卡、小卡、纯文档卡）→ 写 `acceptance_reviewed_by: 不适用（理由）`

> 与执行后测试解耦：评审是静态的，不替代 R03 后续的测试执行。

```
PM产出任务卡 → PM拉起角色子代理并注入任务卡 → 角色执行
                                                    ↓
                       PM验收 ← 子代理回报完成（回报写入 reports/）
                         ↓
                    通过 → 归档
                    不通过 → PM向子代理发出驳回指令
```

### 3.1 PM 产出任务卡

PM 根据 `docs/architecture.md` 的模块分解和 `docs/project-status.md` 的当前状态创建新任务卡，写入 `task-cards/TC-{序号}-{简述}.md`。**落盘硬约束：建卡/回填（`new-worktree.ps1` 回填 front-matter）后必须立即 commit 到主工作区（默认 `develop`）——任务卡是 SSOT 的一部分，未进集成分支的任务卡无法进入 feature Worktree，验收与合并闭环会断裂（`new-worktree.ps1` 已加入前置校验，未提交会报错）**。

**粒度原则**：
- 一张卡 = 一件事，PM 能独立验收
- 预计超过 5 轮对话才能完成 → 拆成两张卡（5 轮为经验启发值，按实际模型能力与任务类型校准；DSH 下 1 轮 ≈ 1 次 subagent run，单卡 = 单次 subagent run 可完成）
- 有串行依赖 → 分别建卡，标注前置依赖

### 3.2 PM 派发任务卡

**手工模式（默认）**：PM 产出派发文本块（角色身份 + 任务卡完整内容），由用户粘贴到对应角色对话；角色完成后在对话按固定格式回报，用户转述给 PM 触发验收。

**自动化模式（DSH 已实测，默认启用）**：见 `docs/dsh-capabilities.md`。DSH 宿主用 `subagent` 一步派发（身份+任务卡）稳定送达，编排驱动见 `docs/dsh-pm-driver.md`；Codex 等宿主 spawn/followup 无法稳定送达，退回手工模式。

**派发后强制落盘 checklist（未完成 = 不算派发完成）**：
1. 运行 `.\scripts\generate-status.ps1` 同步 `project-status.md`、`thread-routing.md` 自动生成区与 `context-brief.md`「当前活跃任务」（脚本自动完成）
2. 在 `docs/thread-routing.md` 活跃对话表登记该角色状态 `working`（运行时句柄用 `list_agents` 查询，不持久化）

> **常驻续卡**：子代理唤起后常驻，后续卡直接 `send_message`（DSH）/ `send_input`（Codex）注入下一张卡，角色保持 `idle` 待命；不关闭、不重新拉起。协议见 `docs/dsh-capabilities.md`「常驻模型」/ `docs/codex-capabilities.md`「常驻模型」。

**手工模式（fallback）**：PM 输出以下派发文本块，由用户粘贴到角色对话：

```
📋 请将以下内容粘贴到 [R0X-岗位名] 对话：

---

执行任务卡：[任务卡内容]

完成后：
1. 按 reports/template.md 格式编写回报
2. 将回报写入任务卡工作目录的 reports/TC-XXX-回报.md（与代码一并提交）
3. 按固定格式回复（含工作目录/分支/提交/工作区状态等定位信息，格式见 `roles/templates/worker-init.md`——完成信号格式唯一权威；缺失 → 标记"定位信息不完整，暂不验收"）
---
```

（手工模式：用户手动粘贴到对应的角色对话。）

### 3.3 监听完成

**自动化模式**：DSH 下后台 subagent 落定时运行时**主动通知父线程**，PM 收到落定通知后触发验收（`list_agents` 确认状态 + `patrol.ps1` 定位回报）；Codex 等无落定通知的宿主才用 `wait_agent` 等待或轮询 `reports/` 目录兜底。
**手工模式（fallback）**：角色回复"TC-XXX 已完成"后，用户回到 PM 对话告知 PM。

### 3.4 PM 验收回报

> **独立审阅预验收（大卡默认）**：大卡正式验收前默认先跑一次独立审阅——用 `subagent` 拉起**新鲜上下文**的审阅子代理（prompt 注入：回报实际路径（`patrol.ps1` 定位）+ 验收标准 + `docs/质量验收标准.md` 四层模型与 §1.2 证据责任矩阵 + 契约引用，可注入 `code-review` / `verification-before-completion` skill 引用），要求运行 `validate-report.ps1`、读回报与相关代码、逐条核查验收标准与外部证据，输出「通过 / 不通过（问题清单）」。结论是 PM 仲裁的**输入**，不自动否决；PM 采纳（驳回/修复卡）或否决（记录理由）均写入 `docs/decisions.md`。涉及测试/契约密集的卡可指定 R03 担任审阅者（原「R03 预验收」路径，并入本机制）。小卡/light 卡豁免。机制详见 `docs/自动化协作协议.md` §3.4。

PM 读取 `reports/TC-XXX-回报.md`：
0. **定位并预校验**：先用 `git worktree list --porcelain` 或 `patrol.ps1` 定位回报文件实际位置（**代码类卡回报在任务卡 Worktree 内的 `reports/`**，不在主工作区副本），再运行 `.\scripts\validate-report.ps1 -Path <实际路径>\TC-XXX-回报.md`，脚本自动检查章节完整性、验收标准勾选与状态一致性
1. **逐条核对验收标准**
2. **质量核查**（不可跳过）：对照 `docs/质量验收标准.md` 四层模型的必查项逐条确认。
   验收标准打勾 ≠ 质量通过。任何必查项未通过 → 驳回
3. **检查契约偏离**：如有偏离，评估是否影响其他角色
4. **记录决策**：任何设计决策写入 `docs/decisions.md`
5. **更新状态**：
   - 通过 → PM 执行 `.\scripts\merge-card.ps1 -Card TC-XXX` 合并（front-matter 状态 accepted → merged），**PM 归档任务卡**到 `task-cards/archive/`，运行 `.\scripts\generate-status.ps1` 更新 `docs/project-status.md` 与 `docs/thread-routing.md` 自动生成区
   - 不通过 → PM 直接向子代理发出驳回指令（手工 fallback 下输出文本块由用户粘贴）
     - 驳回计数 ≥2 仍未收敛 → 按 `docs/自动化协作协议.md` §3.4 驳回收敛协议升级处置（重写任务卡 / 换角色 / 拆卡），理由记入 `docs/decisions.md`

---

### 3.5 复盘与估算校准（每 10 张卡 / 阶段末）

> 触发时机：每完成 10 张任务卡（10 张为经验节奏，可按项目规模/复杂度调整），或每个阶段结束（产品设计 → UIUX → 原型 → 架构 → 开发 → 验收），或发生重大返工/事故后。

**流程**：
1. PM 汇总输入：`reports/` §9 度量信息（实际轮数/耗时/返工次数/主要耗时环节）、Bug 统计（P0-P3 分布）、契约偏离记录、`docs/decisions.md`
2. 按 `docs/retrospectives/template.md` 产出复盘文档，写入 `docs/retrospectives/RT-{序号}-{主题}.md`
3. **估算校准**：计算「预计轮数 vs 实际轮数」偏差率，更新到复盘文档的校准表；后续建卡时 PM 用最近 3 次同类任务的偏差修正「预计工期」
4. 流程改进点：可落地的 → 直接更新相关 docs / 角色文件；需决策的 → 记入 `docs/decisions.md`
5. 复盘文档登记到 `docs/retrospectives/README.md` 索引

> 原则：复盘不是仪式——产出必须包含「下一批任务的估算修正值」与「至少 1 条可执行的流程改进」，否则视为未完成。
## 四、原型确认处理

R04 阶段一产出可交互原型后，PM **向用户汇报原型结果并收集确认/反馈**（人类决策节点，不自动放行）。用户可能不满意，PM 先判断根因：

**判断方法**：打开 R11 的设计规范文档，逐项对照原型——不符合规范的是"实现偏差"，规范本身不合理的是"设计问题"，跟规范和原型都无关的纯新想法是"需求变更"。

### 实现偏差 → 驳回 R04
PM 直接向 R04 子代理发出驳回指令，附具体调整点（手工 fallback 下输出文本块由用户粘贴）。R04 修改原型后重新确认。

### 设计问题 → 驳回 R11
PM 产出任务卡给 R11 调整设计规范。R11 更新后，PM 重新产出派发指令给 R04 按新规范修改原型。

### 需求变更 → 走正式 CR 流程

原型确认通过 = 设计层已冻结。之后的任何需求变更，不走「回溯 R10」，而是走 **`docs/变更管理流程.md`**：

1. PM 分级判定（Level 0/L1 快通道 / Level 2 正式 CR）
2. Level 2 → 生成 CR → 影响分析 → 用户确认代价 → 按层派发
3. Level 0（文案/颜色小调整）→ 直接告知 R04 调整

> **关键**：冻结点之后，只有 PM 有权判定是 Level 0/L1/L2，角色不能自行决定。

### PM 收到 Bug 后的操作

```
1. 读 Bug 报告，确认严重程度（P0 阻断 / P1 严重 / P2 中等 / P3 轻微）
2. 用诊断表（见 §九）定位责任角色
3. 创建 Bug 修复任务卡 → 产出派发指令
4. 自动化：注入给对应子代理；手工 fallback：你粘贴到对应角色对话
5. 角色修复 → 写回报 → PM 验收
6. PM 派 R03 回归测试确认 Bug 已修复
7. R03 回归通过 → PM 关 Bug → 更新 project-status.md
```

**优先级**：P0 阻断 → 立即开卡，暂停当前所有工作。P1-P3 → 排入下一轮任务卡。

### 任务卡已归档、原角色对话已关闭时

Bug 永远派给原责任角色修复——和对上下文最熟。对话关闭不影响：

```
1. PM 重新拉起同编号子代理（命名固定 `R02-后端工程师`），注入初始化指令（`roles/templates/worker-init.md`）
2. 角色按 AGENTS.md 启动流程恢复全貌（读 architecture.md/project-status.md/decisions.md）
3. 注入 Bug 修复任务卡 → 角色执行 → 回报 → 验收 → R03 回归
（手工 fallback：用户在侧边栏新建对话并粘贴初始化指令与任务卡）
```

> 能这样做的原因：项目真相全在 `docs/`、`task-cards/`、`reports/` 和 git 仓库里，不在聊天记录里。**已落盘内容的恢复是 100% 的**；进行中的中间状态（未 commit 代码、子代理上下文）不在此列——重启恢复的完整规则见第五章。

---

## 五、PM 重启与上下文管理

> 本项目的编排模型是 **PM 主编排线程 + 常驻子代理**：子代理唤起后常驻、不关闭，生命周期随 PM 会话存续。因此"上下文过长 → 重启 PM"是本项目的**正常操作**，不是事故——重启后旧子代理失效，重新唤起并让角色从 docs 恢复认知。

### 5.1 何时重启

- PM 上下文接近上限，影响编排判断
- PM 对话丢失 / 卡死 / 异常
- 需要更换模型或新的会话环境

### 5.2 重启前强制步骤（未完成不得重启）

1. **落盘全部活跃子代理的中间状态**（常驻模型下不 `close_agent`，父重启后子代理自然失效）；落盘前，要求每个还在 working 的子代理：
   - 已 commit 全部代码（工作区干净）
   - 已写入回报草稿（任务卡状态标 `partial` / `blocked`，不标 `done`）
2. **落盘 PM 状态**：更新 `docs/project-status.md`（阶段/进行中/已完成/阻塞）、`docs/thread-routing.md`（子代理状态与 agent id）、`docs/decisions.md`（本轮决策）
3. **清点未完成任务卡**：核对 `task-cards/` 中 in_progress / todo 卡状态与实际是否一致

> 原则：**先落盘，再重启**。未落盘 = 没发生（AGENTS.md SSOT 规则）。

### 5.3 重启后恢复步骤

1. 新建 PM 对话，按 §2 SOP 读取：`context-brief.md` → `project-status.md` → `decisions.md` → `thread-routing.md` → `task-cards/` → `reports/`
2. 运行 `.\scripts\patrol.ps1` 核对状态（未验收回报、未合并分支、占位检查）
3. 对需要继续的任务：重新 `subagent` 拉起对应角色子代理（**旧 subagentId 已失效**），注入 `worker-init.md` + 任务卡，角色按 AGENTS.md 从 docs 恢复认知后继续

### 5.4 可恢复范围（明示边界）

| 内容 | 重启后是否可恢复 |
|------|----------------|
| `docs/`、`task-cards/`、`reports/` 中已写入的内容 | ✅ 100% 可恢复 |
| 已 commit 的代码（git） | ✅ 100% 可恢复 |
| 子代理正在执行但未 commit 的中间状态 | ❌ 不可恢复 |
| 未写入 docs 的对话决策 | ❌ 不可恢复（没写入 = 没发生） |

### 5.5 上下文检查点（防患于未然）

- 每完成 3-5 张任务卡（或每轮重要验收后），PM 强制更新 `project-status.md` + `decisions.md`
- 上下文接近上限时**主动重启**（按 5.2 / 5.3），不要等到对话卡死
- 子代理常驻随父会话存续；父 agent 重启后重新唤起（角色从 docs 恢复认知）

---

## 六、契约变更处理

当某个角色的回报中标记了"契约偏离"：

1. **评估严重性**：是设计缺陷还是实现便利性选择？
2. **影响分析**：哪些已完成/进行中的任务卡受影响？
3. **决策**：
   - 小调整 → PM 直接更新 `docs/architecture.md`，产出同步通知给受影响角色
   - 大调整 → 先给 R01 架构师开任务卡，让架构师评估后再更新
4. **通知**：PM 直接通知受影响子代理（手工 fallback 下输出文本块由用户粘贴）

---

## 七、并行编排

> **默认模型**：每个角色只保留一个常驻子代理；该角色的多张卡按依赖/优先级顺序用 `send_message`（DSH）/ `send_input`（Codex）续派，不重复拉起。并行是优化手段，不是默认工作方式。

多张任务卡可以并行时：

1. **耦合检查**：按 `docs/自动化协作协议.md` §3.6 并行耦合检查核对四条耦合通道（文件 / 符号 / 知识 / 上下文），高耦合不并行
2. **并行实例规则**：
   - **不同角色**：各自拉起/复用其常驻子代理，分别注入任务卡，互不冲突。
   - **同一角色**：默认不并行，多张卡顺序续派；**仅当**同时满足以下条件，才可额外拉起临时并行实例：
     a. 该角色有 ≥2 张卡，且顺序执行会造成可感知的关键路径阻塞（交付期限、下游依赖、长任务等）；
     b. 通过上述四条耦合检查；
     c. 每张卡有独立 worktree，文件范围不重叠；
     d. 临时实例显式命名并登记（如 `R02-后端工程师#TC-007`），在 `docs/thread-routing.md` 标记为“临时并行实例”；
     e. 该卡验收合并后临时实例回收/废弃，常驻实例保持唯一。
   - 拿不准是否值得并行时，默认串行。
3. **PM 为每张并行卡创建独立 worktree**（所有代码类任务卡一律 worktree，串行也不例外；用 `.\scripts\new-worktree.ps1 -Number {XXX} -Summary {简述}`，自动创建分支/基线提交/工作目录并回填任务卡 front-matter；规则见 `docs/git-workflow.md` §4），`validate-task-card.ps1` 会校验工作目录为独立 worktree 路径
4. **哪个先完成先验收哪个**（先后完成先后验收，按依赖顺序合并）

（手工 fallback：PM 产出多份派发指令，用户逐个粘贴）

---

## 八、人类决策节点（用户只需在这些时机介入）

自动化模式下，PM 自动编排子代理；**用户仅在以下重要节点决策**：

| 节点 | 用户做什么 |
|------|-----------|
| 项目初始化 | 确认角色组合、确认初始化计划 |
| 原型确认（R04 阶段一） | 逐页查看原型，确认或反馈修改意见 |
| 需求变更 CR | 确认变更代价 |
| 产品最终验收 | 确认整体验收或提出不满意点 |
| 技术/设计方向确认（R01 技术路线、R10 产品方案、R11 设计方向） | 查看方案对比，确认方向后再进入下一步（对应各角色 brainstorming 流程） |
| 重大方向决策 | PM 汇报 → 用户拍板 |

### 关键节点判定规则（PM 自主 vs 必须确认）

| 类型 | 判定 | 示例 |
|------|------|------|
| **必须确认** | 影响"做什么 / 花多少钱 / 什么时候好" | CR Level 2、原型确认、产品最终验收、初始化角色组合、重大方向决策 |
| **PM 自主** | 只影响"怎么做" | 技术实现细节、任务卡拆分、常规验收、L0/L1 变更、Bug 修复卡派发 |
| **拿不准** | 一律确认（宁可多问一次，不可擅自决定） | 无法归类的事项 |

> 判定原则：**影响"做什么 / 花多少钱 / 什么时候好"的，必须问用户；影响"怎么做"的，PM 自主。**

### 关键节点对抗审查（PM 仲裁前必做）

PM 在冻结 PRD / 设计规范 / 架构契约前、大卡验收前、重大方向决策前，先运行 `llm-council` 做对抗审查，拿到「反对理由 / 反例 / 未考虑的失败模式」后再仲裁：

- 委员会意见是 PM 的仲裁输入，不自动否决产出
- 采纳 → PM 产出对应修复/调整卡，或把风险记入 `docs/decisions.md`
- 否决 → 记录理由，驳回对应角色
- 影响「做什么 / 花多少钱 / 什么时候好」的，PM 仍需向用户汇报、由用户拍板（委员会不能替代用户决策）

### R04 原型确认流程（你最重要的检查点）

```
PM 汇报原型已完成（附链接/预览）
        ↓
你逐页浏览，逐项检查：布局 → 导航 → 组件 → 响应式
        ↓
  满意？→ 回复"原型确认，进入阶段二"（PM 据此放行）
  不满意？→ 描述问题（见第四节），PM 判断根因后精准派发
```


### 自动化编排循环（自动化模式的日常运转）

1. **开局**：开 PM 对话，读取状态，唤起角色子代理（常驻），产出首批任务卡
2. **派发**：PM 通过 `send_message` 注入任务卡给子代理
3. **等待**：DSH 下等后台 subagent 落定自动通知；无落定通知的宿主（Codex）轮询 reports/
4. **验收**：子代理回报后 PM 自动触发验收；重要节点向用户汇报
5. **循环**：继续产出下一批任务卡

---

## 九、最终验收不通过的处理

所有开发完成后，你最终验收时可能不满意。和原型确认一样——先诊断，再精准派发。

### 诊断速查表

| 你看到的 | 大概率是 | 在哪个对话反馈 |
|---------|---------|-------------|
| 颜色/间距/布局和原型不一样 | R04 实现偏差 | R04 对话 |
| 按钮没反应/跳转不对 | R04 交互逻辑 | R04 对话，如涉及设计规范则先 R11 |
| 数据不对/报错/返回为空 | R02 后端 | R02 对话 |
| 功能流程不合理 | 产品逻辑问题 | PM 对话，PM 会判断回溯 R10 还是 R01 |
| 慢/卡顿 | 性能问题 | PM 对话，PM 诊断后定位 |

### 操作步骤

1. 回到 PM 对话，描述不满意的具体表现（越具体越好，截图/复现步骤）
2. PM 读文档诊断根因，产出精准任务卡（只给有问题的角色，不改其他角色已完成的工作）
3. 自动化：注入给对应子代理；手工 fallback：你粘贴到对应角色对话
4. 角色修复 → 回报 → PM 验收 → 你再确认

> 如果不满意的原因属于**功能需求变更**（非 Bug、非实现偏差），PM 将启动 `docs/变更管理流程.md` 的正式 CR 流程，而非直接出修复卡。

## 十、常见坑


| 坑 | 避免方法 |
|----|---------|
| 任务卡太模糊，AI 自由发挥过头 | 限制范围写死，禁止修改范围写清楚 |
| 多角色改了同一个文件 | 任务卡备注"文件 X 已被 R0X 锁定，请绕行" |
| AI 回报说"完成了"但没按格式 | PM 产出驳回指令要求重报 |
| 契约悄悄变了，下游不知道 | 契约变更 PM 必须产出通知文本块同步给受影响角色 |
| 子代理完成未被发现 | PM 定期巡检 `reports/` 目录与子代理状态（`patrol.ps1`） |
| 冻结点之后未经 CR 直接改需求 | 任何涉及已冻结产出物的变更，PM 必须先执行 `docs/变更管理流程.md` 的 CR 流程，不可直接派发 |
| 验收标准全打勾但质量不达标 | 验收标准通过后必须执行 `docs/质量验收标准.md` 的质量核查——验收标准 = 功能底线，质量核查 = 上线底线 |
| 对话中做的决策没写入 docs | PM 的 SSOT 维护职责：对话中的确认/驳回/变更决策，必须由 PM 写入 docs 后才成为正式事实。没写入 = 没发生 |

---

## 十一、框架经验记录（lessons.md）

> 新项目是框架的"试验场"——运行中遇到的框架问题记录在 `lessons.md`，由用户复制回框架仓库，驱动框架自身优化。格式与约定见项目根目录 `lessons.md`。

### 项目侧（本手册适用时）

- 运行中遇到**框架问题**（流程卡壳、脚本缺陷、文档矛盾、模板冗余、校验规则误报）：**角色**在完工回报 §8「需要 PM 关注的决策」或对话中报告；**PM 汇总**写入本项目根目录 `lessons.md`（本文件位于主工作区，归 PM 专用，角色不直接写）。格式：`[open] 问题 / 证据 / 建议`，一条一行。
- **框架学习**：本项目 `lessons.md` 由用户直接复制回框架仓库（覆盖或追加，无需合并/标记仪式），框架迭代时读取处理。
- 业务需求、项目级决策**不记入** lessons.md（那是 `docs/decisions.md` 的职责）。

### 框架仓库侧（框架迭代时）

- 框架仓库维护者（R99）**每次迭代前先读 `lessons.md`**，处理所有 `[open]` 条目：
  - 采纳 → 落地改动（改文档 / 脚本 / 模板）→ 条目标 `[done]` 并注明改了什么
  - 不采纳 → 标 `[done] 不采纳（理由）`
  - 暂时处理不了 → 保留 `[open]`，不删除
- 处理完的条目保留在文件中（历史即轨迹），不删行。




