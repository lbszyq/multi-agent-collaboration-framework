---
task_id: TC-{序号}
role: {R0X-岗位名}
time: {YYYY-MM-DD HH:MM}
status: done
light: false
repo_root: {D:/项目/项目名}
workdir_type: worktree
worktree: {D:/项目/项目名-TC{序号}}
base_commit: {abc1234}
branch: feature/TC-{序号}-{简述}
head_commit: {def5678}
workspace_status: clean
---

# 完工回报：TC-{序号} - {任务简述}

> **元数据说明**：任务卡编号、角色、状态、Git 定位信息以文件头 YAML front-matter 为**唯一权威**（机器可读），`scripts/validate-report.ps1` 与 `patrol.ps1` 均读取 front-matter。工作区有未提交修改时 `workspace_status` 填 `dirty` 并在 §1 列出文件及原因，任务状态只能为 `partial` / `blocked`，不得填 `done`。

## 1. 总体状态（状态向量）

> **能观测性**：PM 仅凭本节即可重构任务真实状态，无需追问。`done` 卡须勾选"完成"并确认无阻塞；`partial` / `blocked` 卡必须如实填写全部字段。

| 状态向量 | 值 |
|---------|-----|
| **完成度** | X%（完成 / 部分完成 / 受阻） |
| **阻塞项** | 无 / 具体阻塞（含阻塞方与需要的处理） |
| **风险等级** | 低 / 中 / 高（附一句理由） |
| **需要的 PM 决策** | 无 / 具体决策点（如验收、改卡、拆卡、换角色） |

## 2. 提交说明

<!-- 一句话描述本次提交做了什么（与 front-matter `head_commit` 对应） -->

## 3. 文件变更清单

| 文件路径 | 操作（新增/修改/删除） | 说明 |
|---------|---------------------|------|
| `code/xxx.py` | 修改 | 实现了 xxx 功能 |
| `code/tests/test_xxx.py` | 新增 | 添加了对应测试 |

## 4. 验收标准逐条核查

<!-- 每条验收标准，如实标注通过/未通过 -->

- [x] 标准 1：通过 —— 已验证，结果符合预期
- [x] 标准 2：通过 —— 截图/日志见下方附件
- [ ] 标准 3：未通过 ——（描述失败原因，附截图/日志）

### 4.1 自动化测试证据（如适用）

<!-- 任务卡「测试要求」为自动化测试时必填；纯文档/纯配置任务卡填「不适用」 -->

- **测试命令**：`{测试命令}`
- **测试结果**：全部通过（X 用例，耗时 Ys）
- **覆盖说明**：核心链路（如列表分页 / 表单校验 / 状态切换）已覆盖，测试文件随代码入库

### 4.2 外部证据清单（必须外部证据的验收项）

<!-- 依据 docs/质量验收标准.md §1.2 证据责任矩阵：
- 角色/R03/CI 责任项（F8/T3/T7/U5/P8/P11）：必须逐条附证据；无证据 = 该项未通过
- 用户·PM 责任项（P2/P3/P9/P10）：填「待外部提供」+ 预计提供时机，不阻塞本卡验收
- 纯文档/纯配置任务填"不适用（理由）" -->

| 验收项 | 责任主体 | 证据状态 | 证据位置/链接 | 备注 |
|--------|---------|---------|--------------|------|
| 例：{验收项} | {角色/R03/CI/用户·PM} | 已附 / 待外部提供 / 豁免 | `artifacts/{证据文件}` | 复现命令：`{复现命令}` |

不适用时：不适用（纯文档/配置任务，理由：xxx）

> 状态为「待外部提供」的行必须写明预计提供时机（如"上线验收时由用户提供"）；状态为「豁免」的行必须引用 `docs/project-status.md` 中的豁免记录。

## 5. Bug 报告

<!-- 测试中发现的缺陷。与技术债务（能工作但不优雅）区分开：Bug = 不工作。 -->

- 无
- 或：
  ```
  - [Bug] 标题
    - 严重程度：[P0-阻断 / P1-严重 / P2-中等 / P3-轻微]
    - 复现步骤
    - 预期 vs 实际
    - 相关代码位置
  ```

## 6. 契约偏离

<!-- 如果严格按契约实现了，填"无偏离"。如果发现契约有问题，填下面的内容： -->

**是否偏离**：[无偏离 / 有偏离]

如有偏离：
- **偏离点**：描述哪里和契约不一致
- **原因**：为什么不一致（契约有歧义？不可实现？实现过程中发现了更好的做法？）
- **建议处理**：建议 PM 如何处理（更新契约？修订任务卡？开新卡？）

## 7. 技术债务 / 已知问题

<!-- 诚实记录：有什么做得不够好、但优先级不高暂时没处理的地方 -->
<!-- 架构级债务由 R01 审阅后登记到 `docs/architecture.md` §12 技术债务登记册 -->

- 无
- 或：xxx 函数的性能可优化，当前可满足需求，建议后续开优化卡
- 或（结构化格式，便于 R01 直接登记）：
  - 问题：xxx
  - 原因：MVP 快速验证
  - 影响：xxx
  - 建议优先级：P1/P2/P3

## 8. 需要 PM 关注的决策

<!-- 开发过程中做了哪些需要 PM 知晓和记录的选择 -->

- 无
- 或：选用了 xxx 方案而非 yyy 方案，原因是...

## 9. 度量信息

<!-- 供 PM 校准估算与复盘（对照任务卡「预计工期」），每 10 张卡建议做一次回顾 -->

- **实际轮数**：X 轮（任务卡预计：Y 轮）
- **实际耗时**：X 天（含等待验收时间）
- **返工次数**：0 / X 次
- **主要耗时环节**：[执行 / 等待验收 / 阻塞 / 其他]


